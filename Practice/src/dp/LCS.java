package dp;

public class LCS {

    /**
     * Goal: Return the length of the longest subsequence common to both strings.
     * A subsequence is a new string generated from the original string with some characters
     * (can be none) deleted without changing the relative order of the remaining characters.
     * * Example: text1 = "abcde", text2 = "ace"
     * Output: 3  (The subsequence is "ace")
     */
    public int longestCommonSubsequence(String text1, String text2) {
        // TODO: Implement using 2D DP matrix [text1.length + 1][text2.length + 1]

        return 0;
    }
}