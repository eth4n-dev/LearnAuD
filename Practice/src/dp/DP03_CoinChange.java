package dp;

public class DP03_CoinChange {

    /**
     * Goal: Return the FEWEST number of coins needed to make up 'amount'.
     * Return -1 if the amount cannot be made up by any combination of the coins.
     * * Example: coins = [1, 2, 5], amount = 11
     * Output: 3 (5 + 5 + 1)
     */
    public int minCoins(int[] coins, int amount) {
        // TODO: Implement using 1D DP array
        // Hint: Initialize array with a value larger than amount (e.g., amount + 1)

        return -1;
    }
}