package graph;

import graph.structures.Edge;

import java.util.*;

public class Kruskal {

    // Goal: Return total MST cost using Kruskal's algorithm (Sort edges + UnionFind)
    public int getMSTCost(int numNodes, List<Edge> allEdges) {
        // TODO:
        // 1. Sort edges by weight
        // 2. Iterate and add edge if it doesn't form a cycle (use UnionFind)

        return 0;
    }
}