package graph;

import java.util.*;

public class Dijkstra {
    // Goal: Return the shortest distance from startNode to endNode.
    // Return -1 if no path exists.
    public int shortestPath(int numNodes, int startNode, int endNode, Map<Integer, List<Edge>> adjList) {
        // TODO: Implement Dijkstra
        // Hint: Almost identical to Prim, but update logic is different.

        return -1;
    }
}