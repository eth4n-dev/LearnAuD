package search;

public class BinarySearch {

    /**
     * Goal: Return the index of 'target' in the sorted array 'arr'.
     * Return -1 if not found.
     * Logic: O(log n) using pointers (low, high, mid).
     */
    public int search(int[] arr, int target) {
        // TODO: Implement Binary Search (Iterative or Recursive)

        return -1;
    }
}